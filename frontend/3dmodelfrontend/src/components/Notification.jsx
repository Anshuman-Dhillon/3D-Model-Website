import React from 'react';
import '../component design/Notification.css'; // We'll create this CSS

function Notification({ title, message, time }) {
    return (
        <div className="notification-item">
            
        </div>
    );
}

export default Notification;
