// Google OAuth signup is handled by the unified googleAuth function in googleLogIn.js
// It auto-creates new users when they sign in with Google for the first time.
export { googleAuth } from "./googleLogIn.js";
