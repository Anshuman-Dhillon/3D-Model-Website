// Model files are now stored in AWS S3 — see config/s3.js and middleware/upload.js
// This schema is no longer needed. File metadata (fileKey, thumbnailKey) is stored
// directly in the Model schema (models/model.js).
